apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-shared-tvu27
  namespace: nfs-tvu27
spec:
  accessModes:
    - ReadWriteMany

  resources:
    requests:
      storage: 1Gi

  storageClassName: ""

  volumeName: pv-nfs-tvu27